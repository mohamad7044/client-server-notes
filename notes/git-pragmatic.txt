# git pragmatic

2009-07-24

(c) Copyright 2009 zamri. All Rights Reserved. 

starting
	- git init
	- git clone

clone - make a copy of another repository,
	check out the master branch

fetch and merge remote repository into local repository
push is to send changes to remote reposit
pull is to fetch changes from remote reposit

git config --global user.name "mohdzamri"
git config --global user.email "mohdzamrimurah@gmail.com"
git config --global color.ui "auto"

we learn by repeatition

chap. 3

mkdir mysite
cd mysite
git init
edit hello.txt
git add hello.txt
git commit -m "add hello"


edit hello.html
git add hello.html
# tel git about new change to track
git status
git commit -m "make changes"
git log -l

# branching
git branch <name> master
edit hello.txt
# still on master
git commit -a

git branch R1.0 master
edit hello.txt
git commit -a

# edit branch
git checkout R1.0
edit hello.txt
git commit -a

## release
git tag V1.0 R1.0
git tag

# rebase
git checkout master
git rebase R1.0

git -d R1.0
git -D R1.0

# branching from 
git branch R1.0.1 V1.0
git checkout R1.0.1

git clone

Chapter4.
git commit -m "hello"
git status


git areas;

working <-> stage <-> repo

git diff # working/stage

git diff --cached # stage/reposit

git diff HEAD # working/repo


# managing files
git mv a b

#ignore
.gitignore # repository
.git/info/exclude # locally

chapter 5 / branches

git branch






